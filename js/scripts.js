var withButtonClass = document.getElementsByClassName('button'),
ourTable = ['button', 'button', 'button', 'button'],
i;

console.log(ourTable.length);

for (i=0; i<ourTable.length; i++)  {
	withButtonClass.innerText = 'Hello!';
	withButtonClass.innerText = 'How';
	withButtonClass.innerText = 'are';
	withButtonClass.innerText = 'you?';

}
